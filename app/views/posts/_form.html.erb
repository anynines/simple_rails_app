<div class="form-group">
<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, class: "text_field_content" %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, class: "text_field_content" %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, class: "text_area_content" %>
  </div>
  <div class="actions">
    <%= f.submit "Save", class: "btn btn-success" %>
  </div>
<% end %>
</div>